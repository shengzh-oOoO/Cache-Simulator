使用方法：
./pipeline_cache_rv64isim
运行模拟器
根据提示输出可执行文件名，例如：../test-code/ack
根据提示选择是否输出指令细节，是否进行单步模式。

这一实现中，实现了三级Cache，并且存在有真实的读写行为。这里没有实现Bypass和PreFetch的功能，默认不进行Bypass和PreFetch。
实现了一部分系统调用，输入整数，输出整数、字符、字符串。其中输出字符串功能存在一些问题（在报告中有具体说明），但是在测试程序中没有影响。
在单步模式下以及在最后检查内存时，访问依然是需要通过Cache的，这对统计周期数没有影响，但是会影响Cache的具体统计信息。（不过要求中并没有说明需要这些统计信息）